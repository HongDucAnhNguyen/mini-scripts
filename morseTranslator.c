#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <limits.h>
#define TABLE_SIZE 1000

unsigned int hashGenerator(const char *key)
{
    unsigned long int value = 0;
    unsigned int i = 0;
    unsigned intkey_len = strlen(key);
}

int main()
{
    /**script to translate input text into morse Code*/

    return 0;
}